---
- hosts: controller
  become: true
  tasks:  
  - name: Initialize the Kubernetes cluster using kubeadm
    command: kubeadm init --ignore-preflight-errors=NumCPU --node-name controller --pod-network-cidr=192.168.0.0/16